eureka:
  client:
     service-url:
        defaultZone: http://localhost:8762/eureka
     register-with-eureka: true
     fetch-registry: true

  instance:
    hostname: localhost

spring:
  security:
    user:
      name: mycarenest
      password: mycarenest
